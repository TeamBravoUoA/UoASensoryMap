"""Deprecated placeholder.

Tests for the ``sensemap`` app now live in the ``sensemap/tests/`` package
(see ``tests/test_api_functional.py`` and ``tests/test_security.py``). This
module is intentionally left empty: it is shadowed by the package of the same
name and only kept to avoid confusion. Do not add tests here.
"""
